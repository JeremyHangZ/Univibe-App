<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 背景颜色 -->
    <solid android:color="#FFFFFF" />  <!-- 背景颜色为白色 -->

    <!-- 圆角 -->
    <corners android:radius="16dp" />  <!-- 设置圆角半径为16dp -->

    <!-- 边框 -->
    <stroke
        android:width="2dp"
        android:color="#CCCCCC" />  <!-- 灰色边框，宽度2dp -->

    <!-- 内边距 (可选) -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp" />
</shape>
